---
title: "1. Positive, Negative, or Zero"
date: "2026-05-27"
description: "Write a C++ program that takes an integer from the user and checks whether the number is positive, negative, or zero. 👉 What to do: Use if, else if, and else..."
tags: ["cpp", "competitive-programming", "flow-control", "problems"]
author: "MD Jaber Hossain Chowdhury"
order: 6.2
---

import { BlogPostHeader } from "@/components/BlogPostHeader";

<BlogPostHeader
  title="1. Positive, Negative, or Zero"
  date="2026-05-27"
  author="MD Jaber Hossain Chowdhury"
  tags={["cpp", "competitive-programming", "flow-control", "problems"]}
/>

Write a C++ program that takes an integer from the user and checks whether the number is positive, negative, or zero.

👉 **What to do:**  
Use `if`, `else if`, and `else` to compare the number with 0 and print the correct message.

---

### **2. Even or Odd Number**

Write a program that asks the user to enter a number and determines whether it is even or odd.

👉 **What to do:**  
Use an `if-else` statement and the modulus operator `%` to check if the number is divisible by 2.

---

### **3. Grade Calculator**

Write a program that takes a student's marks (0–100) and prints the grade:

- A (90–100)
    
- B (80–89)
    
- C (70–79)
    
- D (60–69)
    
- F (below 60)
    

👉 **What to do:**  
Use multiple `else if` conditions to check the range of marks and print the correct grade.

---

### **4. Largest of Two Numbers**

Write a program that takes two numbers and prints the larger one.

👉 **What to do:**  
Use `if-else` to compare the two numbers and display the larger value.

---

### **5. Largest of Three Numbers**

Write a program that takes three numbers and finds the largest among them.

👉 **What to do:**  
Use multiple `if-else if` statements to compare all three numbers.

---

### **6. Leap Year Checker**

Write a program that checks whether a given year is a leap year or not.

👉 **What to do:**  
A year is a leap year if:

- It is divisible by 4 and not divisible by 100,  
    OR
    
- It is divisible by 400.  
    Use `if-else` conditions to implement this logic.
    

---

### **7. Voting Eligibility**

Write a program that asks the user’s age and checks if the person is eligible to vote.

👉 **What to do:**  
If age is 18 or above, print “Eligible to vote”, otherwise print “Not eligible”.

---

### **8. Simple Calculator**

Write a program that takes two numbers and an operator (`+`, `-`, `*`, `/`) and performs the calculation.

👉 **What to do:**  
Use `if-else if` to check which operator the user entered and perform the correct operation.

---

### **9. Check Character Type**

Write a program that takes a character as input and checks whether it is:

- A vowel
    
- A consonant
    
- A digit
    
- A special character
    

👉 **What to do:**  
Use multiple `if-else if` conditions to check the character range and type.

---

### **10. Temperature Check**

Write a program that asks the user to enter temperature in Celsius and prints:

- “Freezing” (below 0°C)
    
- “Cold” (0–15°C)
    
- “Warm” (16–30°C)
    
- “Hot” (above 30°C)
    


